apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ .Values.focus.serviceAccount.name }}
  namespace: focus
  annotations:
    eks.amazonaws.com/role-arn: {{ .Values.focus.serviceAccount.roleArn }}